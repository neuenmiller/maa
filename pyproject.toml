[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "maa"
version = "0.0.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "imageio",
    "imageio-ffmpeg",
]

[tool.setuptools]
packages = ["maa"]
